<h1>Listing call_types</h1>

<table>
  <tr>
    <th>Call type</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @call_types.each do |call_type| %>
  <tr>
    <td><%= call_type.call_type %></td>
    <td><%= link_to 'Show', call_type %></td>
    <td><%= link_to 'Edit', edit_call_type_path(call_type) %></td>
    <td><%= link_to 'Destroy', call_type, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Call type', new_call_type_path %>
