<h1>Listing call_locations</h1>

<table>
  <tr>
    <th>Call location</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @call_locations.each do |call_location| %>
  <tr>
    <td><%= call_location.call_location %></td>
    <td><%= link_to 'Show', call_location %></td>
    <td><%= link_to 'Edit', edit_call_location_path(call_location) %></td>
    <td><%= link_to 'Destroy', call_location, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Call location', new_call_location_path %>
